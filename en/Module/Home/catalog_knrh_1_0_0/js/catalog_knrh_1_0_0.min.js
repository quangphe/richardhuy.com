document.addEventListener("DOMContentLoaded",(function(){const box=document.querySelector(".catalog_knrh_1_0_0__box"),items=document.querySelectorAll(".catalog_knrh_1_0_0__item"),dotsContainer=document.createElement("div");dotsContainer.classList.add("dots");let currentIndex=0,slideInterval;function updateDots(){const dots=document.querySelectorAll(".dot");dots.forEach((dot,index)=>{dot.classList.toggle("active",index===currentIndex)})}function goToSlide(index){currentIndex=index;const offset=100*-currentIndex;items.forEach(item=>{item.style.transform=`translateX(${offset}%)`}),updateDots()}function autoSlide(){currentIndex=(currentIndex+1)%items.length,goToSlide(currentIndex)}function startAutoSlide(){slideInterval=setInterval(autoSlide,5e3)}function stopAutoSlide(){clearInterval(slideInterval)}function checkWidth(){window.innerWidth<=430?(goToSlide(0),updateDots(),startAutoSlide()):(items.forEach(item=>{item.style.transform="translateX(0)"}),dotsContainer.style.display="none",stopAutoSlide())}items.forEach((item,index)=>{const dot=document.createElement("span");dot.classList.add("dot"),dot.addEventListener("click",()=>goToSlide(index)),dotsContainer.appendChild(dot)}),box.parentNode.appendChild(dotsContainer),checkWidth(),window.addEventListener("resize",checkWidth)}));